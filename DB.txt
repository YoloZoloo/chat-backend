CREATE USER 'user'@'localhost' IDENTIFIED BY 'mypass';
CREATE DATABASE 'db-name';
use 'db-name';
CREATE TABLE user_m (
    id int PRIMARY KEY AUTO_INCREMENT,
    user_id varchar(18) NOT NULL,
    firstname varchar(18) NOT NULL,
    lastname varchar(18),
    icon blob,
    adminAuthority tinyint NOT NULL,
    password varchar(100) NOT NULL
);
ALTER TABLE user_m ADD UNIQUE (user_id);

CREATE TABLE grouproom_m (
    grouproom_id int,
    guest_id int NOT NULL
);

CREATE TABLE groupchat_t (
    message_id int PRIMARY KEY AUTO_INCREMENT,
    message varchar(1000),
    sender_id int,
    grouproom_id int,
    datetime TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

CREATE TABLE privateroom_m (
    privateroom_id int PRIMARY KEY AUTO_INCREMENT,
    idA int,
    idB int
);

CREATE TABLE privatechat_t (
    message_id int PRIMARY KEY AUTO_INCREMENT,
    message varchar(1000),
    sender_id int,
    privateroom_id int,
    datetime timestamp DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

